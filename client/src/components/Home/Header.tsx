import React from "react";
import styled from "styled-components";

const Header = ({ city, country }: { city: string; country: string }) => {
  return <Container>sacsa</Container>;
};

export default Header;

const Container = styled.div`
  display: flex;
`;
